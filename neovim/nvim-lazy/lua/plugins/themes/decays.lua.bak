return {
	"decaycs/decay.nvim",
	as = "decay",

	config = function()
		require("decay").setup({
			style = "dark",
			nvim_tree = {
				contrast = true, -- or false to disable tree contrast,
			},
		})
	end,
}
